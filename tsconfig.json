{
  "extends": "tsconfig-kodefox",
  "compilerOptions": {
    "jsx": "react-native"
    // "typeRoots": ["node_modules/@types", "custom_types"] /* List of folders to include type definitions from. */
  },
  "types": ["react", "react-native"]
}
